\* DESIGN BASELINE (BIG): W=4 > QCap=2 forces staging. Same good knobs
\* as DesignBaseline but exercises the staging path. EXPECT: ALL PASS.
SPECIFICATION Spec
CONSTANTS
  NumChanges          = 4
  W                   = 4
  QCap                = 2
  StageCap            = 3
  BudgetClock         = "produce"
  GateResetFrame      = FALSE
  VersionCommitOnOpen = FALSE
  AdvanceStages       = TRUE
  FlushBeforePark     = TRUE
  ClientMayStop       = TRUE
  MayAbort            = TRUE
INVARIANTS
  TypeOK
  RowConservation
  VersionRowAtomicity
  NoClientInducedReset
PROPERTIES
  ResetReaches
  AdvanceTerminates
CHECK_DEADLOCK FALSE
